# PostgreSQL (Prisma)
DATABASE_URL=
DIRECT_URL=

# Session signing (required in production — use a long random string)
AUTH_SECRET=

# Optional: first admin user created by `npm run db:seed`
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=
