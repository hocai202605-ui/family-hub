/** SVG viewBox for the Vietnam choropleth (must match `scripts/build-vietnam-map.mjs`). */
export const MAP_WIDTH = 560;
export const MAP_HEIGHT = 840;
export const MAP_VIEWBOX = `0 0 ${MAP_WIDTH} ${MAP_HEIGHT}`;
